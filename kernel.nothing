;; -*- mode: scheme -*-

(define (putc ch)
  (! #x101f1000 ;; UART0
     ch))

(define (main)
  (char-loop 0))

(define (char-loop counter)
  (putc (+ counter 65))
  (char-loop (and 31 (+ counter 1))))
